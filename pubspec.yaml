name: flow_money
description: "FlowMoney — personal finance tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.4.0
  path: ^1.8.3

  cupertino_icons: ^1.0.2
  fl_chart: ^0.65.0
  intl: ^0.19.0

  http: ^1.1.0
  shared_preferences: ^2.5.0
  share_plus: ^10.1.4
  file_picker: ^8.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
